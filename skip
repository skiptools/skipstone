#!/usr/bin/env bash
# Convenience script to run the skip CLI built from source.
# Usage: ./skip [args...]   e.g.  ./skip --help  or  ./skip checkup
set -e
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec swift run --package-path "$SCRIPT_DIR" SkipRunner "$@"
